import styled from 'styled-components';

export const Button = styled.button`
  outline: none;
  border: none;
  border-radius: 0.4rem;
  border: 0;
  outline: none;
  height: 4rem;
  padding: 0 2rem;
  font-weight: 800;
  font-size: 1.8rem;
  cursor: pointer;
`;
